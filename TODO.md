# TODO

## Done
- Added `database/seeders/CompanySeeder.php` to create/update 3 sample companies in `companies` table.

## Next steps
- [x] Update `database/seeders/DatabaseSeeder.php` to call `CompanySeeder`.

- [ ] Run migrations + seeding (`php artisan migrate` and `php artisan db:seed`).
- [ ] Verify companies exist in DB / UI.

