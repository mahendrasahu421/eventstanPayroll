@extends('layouts.app')&#10;&#10;@section('title', 'Advance #' . $advance->id)&#10;&#10;@section('content')&#10;<div class="row">&#10;    <div class="col-md-8">&#10;        <div class="card">&#10;            <div class="card-header d-flex justify-content-between align-items-center">&#10;                <h4 class="mb-0">Advance Payment #{{ $advance->id }}</h4>&#10;                <span class="badge fs-6 {{ $advance->status == 'completed' ? 'bg-success' : 'bg-warning' }}">{{ ucfirst($advance->status) }}</span>&#10;            </div>&#10;            <div class="card-body">&#10;                <div class="row mb-4">&#10;                    <div class="col-md-6">&#10;                        <strong>Employee:</strong> {{ $advance->employee->full_name }}<br>&#10;                        <strong>Code:</strong> {{ $advance->employee->employee_code }}<br>&#10;                        <strong>Department:</strong> {{ $advance->employee->department?->name }}&#10;                    </div>&#10;                    <div class="col-md-6 text-md-end">&#10;                        <h3 class="text-primary mb-1">{{ number_format($advance->amount) }}</h3>&#10;                        <strong class="me-2">Pending:</strong><span class="badge {{ $advance->pending_amount > 0 ? 'bg-warning' : 'bg-success' }} fs-6">{{ number_format($advance->pending_amount) }}</span>&#10;                    </div>&#10;                </div>&#10;&#10;                <!-- Recovery Schedule -->&#10;                @if($advance->recoveries->count())&#10;                <h6 class="mb-3"><i class="bi bi-calendar3 me-2"></i>Recovery Schedule</h6>&#10;                <div class="table-responsive">&#10;                    <table class="table table-sm">&#10;                        <thead><tr><th>Payroll Month</th><th>Amount Recovered</th><th>Payroll Status</th></tr></thead>&#10;                        <tbody>&#10;                            @foreach($advance->recoveries as $recovery)&#10;                            <tr>&#10;                                <td>{{ $recovery->payrollRecord?->payroll_month ?? '-' }}</td>&#10;                                <td>{{ number_format($recovery->amount) }}</td>&#10;                                <td><span class="badge bg-info">{{ $recovery->payrollRecord?->status ?? 'N/A' }}</span></td>&#10;                            </tr>&#10;                            @endforeach&#10;                        </tbody>&#10;                    </table>&#10;                </div>&#10;                @endif&#10;            </div>&#10;        </div>&#10;    </div>&#10;&#10;    <div class="col-md-4">&#10;        <a href="{{ route('advances.index') }}" class="btn btn-outline-secondary w-100 mb-2"><i class="bi bi-arrow-left"></i> Back</a>&#10;        @if($advance->status != 'cancelled')&#10;        <form method="POST" action="{{ route('advances.destroy', $advance) }}" class="mb-2">&#10;            @csrf @method('DELETE')&#10;            <button type="submit" class="btn btn-outline-danger w-100" onclick="return confirm('Cancel this advance?');"><i class="bi bi-x-circle"></i> Cancel</button>&#10;        </form>&#10;        @endif&#10;    </div>&#10;</div>&#10;&#10;<div class="row mt-4">&#10;    <div class="col-12">&#10;        <h6 class="mb-2"><i class="bi bi-info-circle me-2"></i>Details</h6>&#10;        <div class="card">&#10;            <ul class="list-group list-group-flush">&#10;                <li class="list-group-item"><strong>Date:</strong> {{ $advance->advance_date->format('d M Y') }}</li>&#10;                <li class="list-group-item"><strong>Installment:</strong> {{ number_format($advance->installment_amount) }} x {{ $advance->total_installments }}</li>&#10;                @if($advance->reason)<li class="list-group-item"><strong>Reason:</strong> {{ $advance->reason }}</li>@endif&#10;                <li class="list-group-item"><strong>Created:</strong> {{ $advance->created_at->format('d M Y H:i') }} by {{ $advance->createdBy?->name }}</li>&#10;            </ul>&#10;        </div>&#10;    </div>&#10;</div>&#10;&#10;@endsection
