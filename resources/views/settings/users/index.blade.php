@extends('layouts.app')&#10;&#10;@section('title', 'Users Management')&#10;&#10;@section('content')&#10;<div class="row mb-4">&#10;    <div class="col-md-6">&#10;        <h2><i class="bi bi-people-fill me-2"></i>System Users</h2>&#10;    </div>&#10;    <div class="col-md-6 text-end">&#10;        <a href="{{ route('users.create') }}" class="btn btn-primary"><i class="bi bi-person-plus"></i> Add User</a>&#10;    </div>&#10;</div>&#10;&#10;<div class="card">&#10;    <div class="card-header">&#10;        <h6 class="mb-0">{{ $users->total() }} users found</h6>&#10;    </div>&#10;    <div class="card-body p-0">&#10;        <table class="table table-hover mb-0">&#10;            <thead class="table-light">&#10;                <tr>&#10;                    <th>Name</th>&#10;                    <th>Email</th>&#10;                    <th>Role</th>&#10;                    <th>Phone</th>&#10;                    <th>Status</th>&#10;                    <th>Last Login</th>&#10;                    <th>Actions</th>&#10;                </tr>&#10;            </thead>&#10;            <tbody>&#10;                @forelse($users as $user)&#10;                <tr>&#10;                    <td><strong>{{ $user->name }}</strong></td>&#10;                    <td>{{ $user->email }}</td>&#10;                    <td><span class="badge bg-{{ $user->role == 'super_admin' ? 'dark' : 'primary' }}">{{ $user->role_label }}</span></td>&#10;                    <td>{{ $user->phone ?? '-' }}</td>&#10;                    <td>&#10;                        @if($user->is_active)&#10;                            <span class="badge badge-active">Active</span>&#10;                        @else&#10;                            <span class="badge badge-inactive">Inactive</span>&#10;                        @endif&#10;                    </td>&#10;                    <td>{{ $user->last_login?->format('d M Y H:i') ?? 'Never' }}</td>&#10;                    <td>&#10;                        <div class="btn-group btn-group-sm">&#10;                            <a href="{{ route('users.edit', $user) }}" class="btn btn-outline-warning"><i class="bi bi-pencil"></i></a>&#10;                            @if($user->id != auth()->id())&#10;                            <form method="POST" action="{{ route('users.destroy', $user) }}" class="d-inline">&#10;                                @csrf @method('DELETE')&#10;                                <button class="btn btn-outline-danger" onclick="return confirm('Delete user?');"><i class="bi bi-trash"></i></button>&#10;                            </form>&#10;                            @endif&#10;                        </div>&#10;                    </td>&#10;                </tr>&#10;                @empty&#10;                <tr><td colspan="7" class="text-center py-4">No users found.</td></tr>&#10;                @endforelse&#10;            </tbody>&#10;        </table>&#10;    </div>&#10;</div>&#10;&#10;{{ $users->links() }}&#10;&#10;@endsection
